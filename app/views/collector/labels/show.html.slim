- if notice
  .alert.alert-success.alert-dismissible role="alert"
    button.close type="button" aria-label="Close" data-dismiss="alert"
      span aria-hidden="true" &times;
    = notice

h3 Label #{ @label.id }

dl.dl-horizontal
  dt Index Number:
  dd = @label.index_number
  dt Name:
  dd = @label.name
  dt Description:
  dd = @label.description
  dt Url:
  dd = @label.url
  dt Twitter:
  dd
    - if @label.twitter.present?
      = link_to "@#{ @label.twitter }", 'https://twitter.com/%s' % @label.twitter, target: '_blank'
  dt Ameblo:
  dd
    - if @label.ameblo.present?
      = link_to "ameblo.jp/#{ @label.ameblo }", 'http://ameblo.jp/%s/' % @label.ameblo, target: '_blank'
  dt Tags:
  dd = @label.tags

h4
  = link_to 'Faces (%d)' % @label.faces.size, collector_label_faces_path(@label)
div
  - @label.faces.all.sample(5).each do |face|
    a href=collector_face_path(face)
      = image_tag image_collector_face_path(face)

hr

= link_to 'Edit', edit_collector_label_path(@label), class: %(btn btn-link)
'|
= link_to 'Back', collector_labels_path, class: %(btn btn-link)
