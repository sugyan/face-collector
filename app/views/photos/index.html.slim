h1 Photos
form.well
  .form-group
    input.form-control name='q' value=params[:q]
  button.btn.btn-primary Search
table.table.table-striped
  - @photos.each do |photo|
    tr
      td
        blockquote class="twitter-tweet" data-cards="hidden" data-conversation="none" data-dnt=true
          a href=photo.url
      td
        p #{ photo.faces.size } faces
        - photo.faces.each do |face|
          a href=face_path(face)
            = image_tag image_face_path(face), width: 84, height: 84
      td
        = link_to 'Destroy', photo, class: %(btn btn-danger), data: { confirm: 'Are you sure?' }, method: :delete
.text-center data-no-turbolink='true'
  = paginate @photos
